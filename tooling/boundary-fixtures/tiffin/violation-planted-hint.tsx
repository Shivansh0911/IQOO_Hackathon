// RULE B fixture: no attributes planted for the agent, no mention of Origo.
export function CartButton() {
  return (
    <button data-origo-id="cart" aria-label="Origo cart">
      Cart
    </button>
  );
}
