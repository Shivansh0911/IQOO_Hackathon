# Keep the dependency graph strict: no phantom deps, so a package that forgets
# to declare something fails locally instead of on a judge's machine.
strict-peer-dependencies=false
auto-install-peers=true
