node_modules/
dist/
build/
coverage/
.vite/
*.tsbuildinfo

# secrets — the OpenRouter key is never committed (brief §3)
.env
.env.*
!.env.example

# WebLLM / MediaPipe model weights are large and cached, never versioned
models/
*.wasm.cache
.webllm/
.cache/

.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# screenshots are evidence, not source
.shots/

# gate 2 measurement: a real browser profile holding the cached model weights
.webgpu-profile/

# a persistent browser profile holding the cached model for demo verification
.demo-profile/

# recorded demo footage — large, regenerate with pnpm record
.shots/video/
